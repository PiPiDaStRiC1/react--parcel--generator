# React Project Generator

CLI tool to generate React project structure with Parcel

## Installation

```bash
npm install -g react-parcel-generator
```

## Usage

```bash
# Create structure in current folder
create-react-structure

# Create structure in new folder
create-react-structure my-project
```